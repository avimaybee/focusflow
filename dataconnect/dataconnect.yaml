specVersion: "v1"
serviceId: "focusflow"
location: "us-east1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "ff"
      cloudSql:
        instanceId: "focusflow"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
